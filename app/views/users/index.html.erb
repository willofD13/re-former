<h1> Users: </h1>

<ul>
    <%  @users.each do |user| %>
    <li>
        <div> <%= link_to user.username, user_path(user.id) %> </div><br>
    </li>
    <% end %>
</ul>

<%= link_to "New user", new_user_path %>